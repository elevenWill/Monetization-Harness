interface:
  display_name: "Experiment Designer"
  short_description: "Design safe real-window transaction tests"
  default_prompt: "Use $experiment-designer to design the cheapest safe experiment, including a real deadline window when relevant."
