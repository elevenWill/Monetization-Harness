# Transferability Check

External evidence proves that a mode may have existed under its original conditions. Test separately whether the current user can reproduce its critical mechanism. This check is mandatory before the orchestrator recommends copying a `Cxxx` or Closest Proven Playbook; it is not satisfied merely because the Case reconstruction lists resources.

## Contents

- [Compare conditions](#compare-conditions)
- [Classify every component](#classify-every-component)
- [Determine the gap](#determine-the-gap)
- [Define a replication experiment basis](#define-a-replication-experiment-basis)
- [Write the Closest Proven Playbook](#write-the-closest-proven-playbook)

## Compare conditions

For each case, compare:

```text
user capabilities
existing channels
reachable customers
capital and downside capacity
available time
supply chain
brand trust
content production
sales capability
platform account conditions
current policy conditions
geographic market
product category
delivery capability
```

Add any case-specific dependency identified during reconstruction: official partnership, privileged data, exclusive distribution, celebrity/persona trust, licensed likeness, mature team, or subsidized acquisition.

Start from the reference mechanism, not a fixed checklist. For each condition
that mechanism materially requires, ask whether the current user can:

- produce the bought result to a minimum acceptance standard;
- access the buyer or audience and execute the acquisition path;
- cross the minimum trust barrier;
- perform the minimum manual version;
- cap or survive delivery and rework liability;
- sustain plausible unit economics during that manual version; and
- replace any critical advantage that the reference Case possessed.

Do not require conditions irrelevant to the mechanism, and do not infer a user
condition from general market evidence.

## Classify every component

```yaml
copyable:
  - mechanism the user can reproduce now with bounded resources
context_dependent:
  - mechanism whose result depends on platform, category, timing, or account context
non_copyable:
  - advantage unavailable within the contemplated test
unknown:
  - condition whose presence or causal importance has not been established
```

Example:

```yaml
copyable:
  - 10-15 second product demonstration structure
  - multiple creative variants per product
  - outcome-based service offer
context_dependent:
  - recommendation traffic
  - product category
  - account history
non_copyable:
  - an existing million-follower audience
  - official platform placement
  - an exclusive supply chain
unknown:
  - whether AI disclosure changes conversion for the target segment
```

## Determine the gap

Name the smallest set of missing conditions that could invalidate transfer. Do not average them into a score. Distinguish:

- market precedent: someone operated or transacted under known conditions;
- policy permission: the exact activity is currently permitted or conditional;
- behavioral support: target users acted, not merely expressed interest;
- transferable mechanism: the causal component can be copied;
- user readiness: the current user has the minimum resources to test it.

Apply the result before choosing an action:

```text
market exists + user readiness evidenced -> bounded replication probe
market exists + blocking condition unknown -> claim-matched migration probe first
market exists + critical condition absent -> do not recommend current replication
market exists + core advantage non-copyable -> adjacent case only
```

Match the probe to the gap: production/delivery readiness uses a no-customer-risk
sample or simulation with explicit acceptance, time, cost, quality, and rework
bounds; buyer access, trust, or acquisition uses bounded pre-delivery Reality
Contact; manual economics uses a costed simulation or internal dry run. An
`unknown` delivery condition is not permission to sell first and learn on the
customer. A small gap that can be tested cheaply is not a permanent rejection.

## Define a replication experiment basis

Pass the following to `experiment-designer`:

```text
Reference Case: Cxxx
Mechanism copied:
Unique advantages not copied:
Migration assumption:
Target customer and payer:
Maximum money/time exposure:
Success criterion:
Failure criterion:
Stop condition:
Evidence captured:
```

Prefer a manual or service-shaped transaction test that preserves the proven mechanism. Do not build a full system merely because an external case used software or AI.

## Write the Closest Proven Playbook

Answer:

1. What is the closest verified mode?
2. Who uses it and who pays?
3. What bought result is sold?
4. How is the customer acquired?
5. How is the result delivered?
6. Which trust and policy conditions matter?
7. Which parts are directly copyable?
8. Which are context-dependent or non-copyable?
9. What does the current user lack?
10. What is the cheapest bounded replication that tests the largest migration assumption?

If only adjacent evidence exists, write:

```text
Exact Proven Playbook:
未找到

Closest Adjacent Playbook:
<adjacent transaction structure>

关键差异:
<why it cannot prove the exact proposed mode>
```
