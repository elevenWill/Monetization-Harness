interface:
  display_name: "Leverage Designer"
  short_description: "Productize repeated value without deadline traps"
  default_prompt: "Use $leverage-designer to turn repeated delivery into leverage while checking deadline capacity and liability."
